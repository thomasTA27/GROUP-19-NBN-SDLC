# Research synthesis

The rollup of findings across everyone's captures in `ai-in-sdlc/` and
`modules/` into conclusions that feed the white paper.

This is not a place for raw captures — those belong in the personal files
in `ai-in-sdlc/` or the module-specific files in `modules/`, using
`TEMPLATE.md`. Add a section here once there's enough findings across
people to support a conclusion, and cite the source captures it draws
from.

<sections to be added as findings accumulate>
